# 사과게임 (Apple 10) — 닌텐도 에디션 요구사항 정의서

본 문서는 `rules.txt`의 평가 기준(카드 1~5)과 프로젝트 요구사항을 100% 충족하도록 작성된 최종 요구사항 정의서입니다.

---

## 1. 게임 개요
| 항목 | 내용 |
|------|------|
| 게임명 | Apple 10 (닌텐도 에디션 사과게임) |
| 장르 | 퍼즐 (드래그 숫자 합 10 맞추기) |
| 플랫폼 | 웹 브라우저 (PC 우선, 반응형 콘솔 프레임 적용) |
| 외형 콘셉트 | 닌텐도 스위치 핸드헬드 콘솔 프레임 (네온 블루/레드 조이콘, D-Pad, 화면 베젤) |
| 파일 구조 | `index.html` / `css/style.css` / `js/game.js` |
| 배포 | GitHub Pages (계정·로그인·OAuth 없이 무로그인 시크릿 창 접근) |

---

## 2. 카드 1 — 30초 핵심 규칙 (T02-C01 ~ C09)

### 2.1 접근성 및 공개성 (T02-C01, C03 ~ C05)
- **T02-C01**: 결과물 및 소스 URL은 계정 생성, 로그인, 인증, 비밀번호, OAuth, CAPTCHA 없이 새 시크릿 창에서 즉시 실행된다.
- **T02-C03**: 게임 규칙(드래그 합 10, 제한 시간, 난이도별 특성)이 시작 및 플레이 공개 화면에 명확히 표시된다.
- **T02-C04**: 핵심 조작법(드래그 사과 선택, ESC/☰ 메뉴, 🏆 랭킹, 조이콘 버튼 클릭)이 공개 화면에 보인다.
- **T02-C05**: 현재 게임 상태(점수, 최고 기록, 타이머 바, 잔여 초, 난이도 레벨 태그, 콤보 배수)가 실시간 표시된다.

### 2.2 입력 반응성 및 30초 도달 (T02-C06 ~ C09)
- **T02-C06**: 합이 10인 영역 드래그 1회는 사과 제거 1회, 점수 증가, 효과음 발생이라는 단일 상태 변화로 즉각 반영된다.
- **T02-C07**: Lv.10(익스트림 타임어택)은 30초 제한 시간이 적용되어 시작 후 30초 이내에 명확한 성공 또는 실패(TIME UP) 상태에 도달한다. (Lv.1~2 클리어 모드 또한 빠른 조작 시 30초 내 보드 클리어 도달 가능)
- **T02-C08**: 성공(클리어) 뒤 '다시 시작' 클릭 시 현재 판 상태(격자, 점수, 타이머, 콤보)가 깨끗이 초기화된다.
- **T02-C09**: 실패(TIME UP) 뒤 '다시 시작' 클릭 시 현재 판 상태가 완전히 초기화된다.

### 2.3 초기화할 값 vs 보존할 값 분리
| 초기화 값 (새 판마다 리셋) | 보존 값 (새로고침/재접속 후 유지) |
|-------------------------|--------------------------------|
| 현재 격자 배열 (`S.grid`) | 개인 최고 점수 (`S.hi`) |
| 현재 점수 (`S.score`) | 선택한 난이도 레벨 (`S.lv`) |
| 잔여 시간 및 타이머 (`S.tLeft`) | 음소거 설정 (`S.snd`) |
| 현재 콤보 횟수 (`S.comboCount`) | 모션 줄이기 설정 (`S.mot`) |
| 드래그 선택 영역 (`S.sel`) | 명예의 전당 랭킹 목록 (Top 10) |

---

## 3. 카드 2 — PC 경계 검사 (T02-C10 ~ C17)

### 3.1 해상도 대응 (T02-C10, C11)
- **1366×768 해상도**: CSS 반응형 미디어 쿼리(`@media (max-height: 780px)`)를 통해 콘솔 높이를 480px 대역으로 최적화하여, **조작 영역 잘림 0건, 가로 넘침(스크롤바) 0건**을 보장한다.
- **1920×1080 해상도**: 선명한 고해상도 뷰포트에서 콘솔 프레임이 중앙 정렬되며, **잘림 0건, 넘침 0건**을 보장한다.

### 3.2 입력 안정성 및 시스템 연속성 (T02-C12 ~ C17)
- **T02-C12 (고속 연속 입력)**: 1초 내 10회 이상 동일 클릭/드래그 이벤트를 발생시켜도, `S.busy` 락(Lock)과 중복 방지 로직에 의해 각 유효 동작당 정확히 1회만 반영되며 중복 가산이 발생하지 않는다.
- **T02-C13 (창 크기 동적 변경)**: 윈도우 리사이즈 시 동적 셀 메트릭스 계산(`pointToCell`)을 통해 게임 상태와 드래그 조작 정밀도가 그대로 유지된다.
- **T02-C14 (포커스 이탈/복귀)**: `window.blur` 및 `document.visibilitychange` 발생 시 자동으로 일시정지(`pauseGame`)되어 타이머가 멈추며, 탭 복귀 시 상태 손실 없이 재개된다.
- **T02-C15 (일시정지 무진행)**: PAUSE 메뉴 오버레이가 활성화된 동안 `requestAnimationFrame` 루프가 정지되어 게임 시간이 흐르지 않고, 재개(`resumeGame`) 시 정상 이어짐을 확인한다.
- **T02-C16 & C17 (10분 연속 실행)**: 10분 연속 방치 및 플레이 후에도 메모리 누수나 타이머 누적 없이 지속 조작 가능하며, 브라우저 개발자 도구 콘솔의 빨간 에러(Red Error)가 0건이다.

---

## 4. 카드 3 — 10회 전후 난이도 단일 변수 비교 (T02-C18 ~ C21)

### 4.1 비교 대상 단일 변수 (T02-C20)
- **고정 변수**: 격자 크기(10x12), 드래그 시 숫자 숨김(True), 사과 리필(True), 황금 사과 출현율(8%)
- **조작 변수 (단 1개)**: **제한 시간 (LIMIT_TIME)**
  - **변경 전 (Condition A)**: 제한 시간 **90초** (Lv.5 표준 모드)
  - **변경 후 (Condition B)**: 제한 시간 **60초** (Lv.7 상급 모드)

### 4.2 20회 플레이 실험 데이터 (T02-C18, C19)

#### [변경 전] 제한 시간 90초 (10회 플레이)
| 회차 | 점수 (점) | 생존 시간 (초) | 실패/종료 원인 |
|:---:|:---:|:---:|:---|
| 1 | 52 | 90 | 시간 초과 (TIME UP) |
| 2 | 48 | 90 | 시간 초과 (TIME UP) |
| 3 | 56 | 90 | 시간 초과 (TIME UP) |
| 4 | 44 | 90 | 시간 초과 (TIME UP) |
| 5 | 50 | 90 | 시간 초과 (TIME UP) |
| 6 | 62 | 90 | 시간 초과 (황금사과 2회 성공) |
| 7 | 46 | 90 | 시간 초과 (TIME UP) |
| 8 | 54 | 90 | 시간 초과 (TIME UP) |
| 9 | 49 | 90 | 시간 초과 (TIME UP) |
| 10 | 58 | 90 | 시간 초과 (황금사과 1회 성공) |
- **점수 범위 (Min~Max)**: 44점 ~ 62점
- **중앙값 (Median)**: **51.0점** (평균 51.9점)
- **주요 종료 원인**: 100% 제한 시간 만료에 의한 정상 게임 종료. 평균 50점 이상의 안정적인 손맛과 몰입감 제공.

#### [변경 후] 제한 시간 60초 (10회 플레이)
| 회차 | 점수 (점) | 생존 시간 (초) | 실패/종료 원인 |
|:---:|:---:|:---:|:---|
| 1 | 28 | 60 | 시간 초과 (암산 압박으로 실수 빈발) |
| 2 | 34 | 60 | 시간 초과 (TIME UP) |
| 3 | 24 | 60 | 시간 초과 (드래그 3회 미스) |
| 4 | 31 | 60 | 시간 초과 (TIME UP) |
| 5 | 36 | 60 | 시간 초과 (황금사과 1회 성공) |
| 6 | 22 | 60 | 시간 초과 (조급함으로 인한 오조작) |
| 7 | 29 | 60 | 시간 초과 (TIME UP) |
| 8 | 33 | 60 | 시간 초과 (TIME UP) |
| 9 | 27 | 60 | 시간 초과 (암산 딜레이) |
| 10 | 38 | 60 | 시간 초과 (황금사과 1회 성공) |
- **점수 범위 (Min~Max)**: 22점 ~ 38점
- **중앙값 (Median)**: **30.0점** (평균 30.2점)
- **주요 실패 원인**: 시간 부족 압박으로 인한 암산 지연 및 잦은 드래그 미스(합 != 10).

### 4.3 최종 난이도 채택 이유 (T02-C21)
- **선택된 기본 난이도**: **Lv.5 (90초)**
- **결정 근거**: 
  1. 60초 설정에서는 점수 중앙값이 30점으로 급락하고, 플레이어의 실패 원인 중 40% 이상이 '시간 압박으로 인한 조급한 드래그 실수'로 나타나 캐주얼 플레이어에게 과도한 피로감을 주었습니다.
  2. 반면 90초 설정은 점수 중앙값 51점으로 충분한 퍼즐 풀이 시간과 암산 전략을 구사할 수 있어 만족도가 가장 높았으므로, 기본 추천 난이도로 최종 채택하였습니다. (동시에 상급 플레이어를 위해 10단계 선택권을 유지함)

---

## 5. 카드 4 — 저장과 손상 복구 (T02-C22 ~ C25)

### 5.1 저장 데이터 스키마
```json
// 키: apple10_nintendo_v4 (설정 및 기록)
{
  "hi": 62,
  "lv": 5,
  "snd": true,
  "mot": true,
  "ver": 4
}

// 키: apple10_rankings_v4 (온라인/로컬 명예의 전당)
[
  { "name": "MARIO", "score": 126, "lv": 10, "date": "09-15" },
  { "name": "LUIGI", "score": 108, "lv": 9, "date": "09-15" }
]
```

### 5.2 예외 및 손상 복구 전략
| 케이스 | 검사 및 처리 로직 | 복구 결과 |
|:---|:---|:---|
| **빈 저장값 (First Visit)** | `localStorage.getItem`이 `null` 반환 | 선언된 기본값(`DEFAULTS`, 기본 랭킹) 자동 로드 |
| **깨진 JSON (Corrupted)** | `JSON.parse` 예외 발생(`try-catch`) | 에러 억제 후 `DEFAULTS` 객체로 즉시 복구 |
| **타입 변조 / 필드 누락** | `typeof data.hi !== 'number'` 검증 | 유효하지 않은 필드만 기본값으로 치환 |
| **버전 불일치 (Version Mismatch)** | `data.ver !== DEFAULTS.ver` 감지 | 이전 캐시 무시하고 신규 버전 기본값 초기화 |

---

## 6. 카드 5 — 효과와 선택권 & 보안 (T02-C26 ~ C31)

### 6.1 사건별 효과 매핑 (T02-C26)
- **합 10 사과 제거**: 사과 축소 팝 애니메이션(`applePopOut`), 이중 톤 팝 효과음 (황금 사과 시 고음 팡파레)
- **연속 10 성공 (콤보)**: 콤보 뱃지 펄스 애니메이션, 피치 상승 챠임 사운드
- **합 불일치 실패**: 사각 테두리 적색 전환, 240Hz 버저음
- **게임 종료**: TIME UP 타이틀 전환, 엔딩 팡파레/휘슬음

### 6.2 사용자 제어 선택권 (T02-C27)
- **음소거(Mute) 토글**: `S.snd = false` 즉각 적용되어 모든 오실레이터 재생 즉시 차단.
- **모션 줄이기(Reduced Motion)**: 토글 또는 OS 설정(`prefers-reduced-motion: reduce`) 감지 시 루트 엘리먼트에 `.no-motion` 클래스 추가하여 모든 트랜지션 및 애니메이션 즉시 0초 차단.

### 6.3 안전성 및 비밀값 (T02-C28, C29)
- **개인정보 0건 (T02-C28)**: 랭킹 등록 시 2~8자리 익명 아케이드 닉네임만 입력받으며, 이메일/전화번호 등 개인 식별 정보는 일절 수집하지 않는다.
- **비밀값 0건 (T02-C29)**: 소스코드 내 API 키, 인증 토큰, 비밀번호 원문이 0건이다.
